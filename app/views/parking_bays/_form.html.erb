<%= simple_form_for :search, url: root_path, method: "GET", html: { class: 'inline' } do |f| %>
  <%= f.input :address,
              input_html: {value: ""}
              #  data: {address_autocomplete_target: "address"}},
              # wrapper_html: {data: {controller: "address-autocomplete",
              # address_autocomplete_api_key_value: ENV["MAPBOX_API_KEY"]}}
  %>
  <%= f.submit "Search", class: "" %>
<% end %>
